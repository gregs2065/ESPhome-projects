esphome:
  name: esp32-s3-freenove
  friendly_name: ESP32-S3 Freenove

esp32:
  board: esp32-s3-devkitc-1
  cpu_frequency: 240MHz
  variant: esp32s3
  flash_size: 16MB
  framework:
    type: esp-idf
    version: recommended
    sdkconfig_options:
      CONFIG_ESP32S3_DATA_CACHE_64KB: "y"
      CONFIG_ESP32S3_DATA_CACHE_LINE_64B: "y"
      CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB: "y"

      # Moves instructions and read only data from flash into PSRAM on boot.
      # Both enabled allows instructions to execute while a flash operation is in progress without needing to be placed in IRAM.
      # Considerably speeds up mWW at the cost of using more PSRAM.
      CONFIG_SPIRAM_RODATA: "y"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "y"

      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"

      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_MBEDTLS_SSL_PROTO_TLS1_3: "y"  # TLS1.3 support isn't enabled by default in IDF 5.1.5

psram:
  mode: octal #Change this to quad for N8R2 and octal for N16R8
  speed: 80MHz

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "SXjwFKrXGr88iPXvnN6CMJ7nqnWNqn/rvLEjoj63aNU="

ota:
  - platform: esphome
    password: "2d2e3a545500fcc917bd42cb630b4c09"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esp32-S3-Freenove"
    password: "HZMAjnIhvxIF"

captive_portal:


image:
  rgb565:
    alpha_channel:
    - file: "images/dogbowl.png"
      resize: 75X75
      id: dogbowl2

spi:
- id: lcd
  clk_pin: GPIO2
  mosi_pin: GPIO1
  # miso_pin: GPIO13

display:
  - platform: ili9xxx
    model: ST7789V #ILI9341 
    spi_id: lcd
    cs_pin: GPIO42
    dc_pin: GPIO41
    reset_pin: GPIO40
    id: my_display
    rotation: 90
    color_palette: 8BIT
    data_rate: 40MHz
    invert_colors: true
    pages:
      - id: test_page
        lambda: |-
          it.image(0, 0, id(dogbowl2));


    